name: build-win-release

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Build label, e.g. win-d7-c1"
        required: false
        default: "win-artifact-only"
  push:
    branches: [ "main" ]
    paths:
      - "core-platform/apps/desktop/**"
      - ".github/workflows/build-win-release.yml"

permissions:
  contents: read
  actions: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build-windows:
    env:
      MAOMIAI_WINDOWS_BUNDLE_TARGETS: msi
      MAOMIAI_BUILD_COMMIT: ${{ github.sha }}
      MAOMIAI_BUILD_REF: ${{ github.ref_name }}
    runs-on: windows-latest

    steps:
      - name: Checkout (without submodules)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            core-platform
            .github
            docs

      - name: Clean up broken submodule references
        shell: pwsh
        run: |
          # Remove .gitmodules if it exists (will be recreated by other workflows but breaks this one)
          if (Test-Path .gitmodules) {
            Write-Host "Removing .gitmodules to prevent git exit code 128"
            Remove-Item .gitmodules -Force
          }
          # Remove any submodule references from .git/config
          if (Test-Path .git/config) {
            $config = Get-Content .git/config -Raw
            $config = $config -replace '(?s)\[submodule "[^"]+"\]\s*\n(\s*path\s*=.*\n\s*url\s*=.*\n?)+', ''
            Set-Content .git/config -Value $config -NoNewline
          }

      - name: Show commit and tree
        shell: pwsh
        run: |
          Write-Host "Commit SHA: $env:MAOMIAI_BUILD_COMMIT"
          Write-Host "Ref: $env:MAOMIAI_BUILD_REF"
          Get-ChildItem core-platform/apps/desktop/src -Recurse | Select-Object -First 100 FullName, Length

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install frontend deps
        working-directory: ./core-platform/apps/desktop
        shell: pwsh
        run: |
          npm install

      - name: Prepare frontend dist from latest source
        working-directory: ./core-platform/apps/desktop
        shell: pwsh
        run: |
          if (Test-Path dist) {
            Remove-Item dist -Recurse -Force
          }
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item src\* dist -Recurse -Force

          $sha = $env:MAOMIAI_BUILD_COMMIT.Substring(0, 7)
          $time = Get-Date -Format o
          $meta = @{
            commit = $sha
            built_at = $time
            workflow = "build-win-release"
            label = "${{ github.event.inputs.tag_name }}"
          } | ConvertTo-Json
          Set-Content -Path dist\BUILD_INFO.json -Value $meta -Encoding UTF8

          if (!(Test-Path dist\index.html)) {
            throw "dist/index.html missing"
          }
          if (!(Test-Path dist\js)) {
            throw "dist/js missing"
          }

          Write-Host "Prepared dist from latest source:"
          Get-ChildItem dist -Recurse | Select-Object -First 150 FullName, Length
          Write-Host "BUILD_INFO:"
          Get-Content dist\BUILD_INFO.json

      - name: Verify D7-C1 UI changes are in dist
        working-directory: ./core-platform/apps/desktop
        shell: pwsh
        run: |
          if (!(Select-String -Path dist\index.html -Pattern "hidden-internal" -Quiet)) {
            throw "D7-C1 hidden-internal marker not found in dist/index.html; Windows package would be old"
          }
          if (!(Test-Path dist\js\auto-start-services.js)) {
            throw "auto-start-services.js missing from dist; Windows package would be old"
          }
          Write-Host "D7-C1 markers verified in dist."

      - name: Debug Tauri config
        working-directory: ./core-platform/apps/desktop
        shell: pwsh
        run: |
          Get-Content src-tauri\tauri.conf.json

      - name: "Windows bundle target: MSI only"
        shell: pwsh
        run: |
          Write-Host "Building MSI only to avoid NSIS download 504 during CI."

      - name: Build Tauri Windows bundle
        working-directory: ./core-platform/apps/desktop
        shell: pwsh
        run: |
          npm run tauri build -- --bundles msi

      - name: Verify Windows artifact contents
        shell: pwsh
        run: |
          Write-Host "Checking generated Windows artifacts..."
          Get-ChildItem -Recurse -File core-platform/releases -ErrorAction SilentlyContinue | Select-Object FullName, Length
          if (!(Test-Path "core-platform/apps/desktop/src-tauri/tauri.conf.json")) {
            throw "tauri.conf.json missing"
          }
          $conf = Get-Content "core-platform/apps/desktop/src-tauri/tauri.conf.json" -Raw
          $required = @(
            "../../../scripts/windows",
            "../../../services/agent_runtime_service",
            "../../../services/model_gateway",
            "../../../services/model_bootstrap_service",
            "../../../services/repo_memory_service",
            "../../../services/skill_store_service",
            "../../../services/workflow_store_service",
            "../../../data/agent_policy",
            "../../../data/sandbox_policy",
            "../../../data/agent_team"
          )
          foreach ($item in $required) {
            if ($conf -notlike "*$item*") {
              throw "Missing resource marker in tauri.conf.json: $item"
            }
          }
          Write-Host "Artifact resource markers OK."

      - name: Collect artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist-win | Out-Null
          $paths = @(
            "core-platform/apps/desktop/src-tauri/target/release/bundle/msi/*.msi",
            "core-platform/apps/desktop/src-tauri/target/release/bundle/nsis/*.exe",
            "core-platform/apps/desktop/src-tauri/target/release/*.exe"
          )
          foreach ($p in $paths) {
            Get-ChildItem $p -ErrorAction SilentlyContinue | Copy-Item -Destination dist-win -Force
          }

          Copy-Item "core-platform/apps/desktop/dist/BUILD_INFO.json" -Destination dist-win -Force

          Write-Host "Collected Windows artifacts:"
          Get-ChildItem dist-win | Select-Object FullName, Length

          if (!(Get-ChildItem dist-win -ErrorAction SilentlyContinue)) {
            throw "No Windows artifacts collected"
          }

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: local-ai-platform-win
          path: dist-win/*
          if-no-files-found: error
